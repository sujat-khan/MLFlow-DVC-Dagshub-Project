from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='using mlflow, dvc, and Dags hub in a single project',
    author='Sujat',
    license='',
)
